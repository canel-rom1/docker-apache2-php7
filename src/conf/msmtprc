# Set default values for all following accounts.
defaults
port <PORT>
tls on
tls_certcheck on
tls_trust_file /etc/ssl/certs/ca-certificates.crt

account def_account
host <HOST>
from <MAILFROM>
auth on
user <USER>
password <PASSWORD>

# Set a default account
account default : def_account
